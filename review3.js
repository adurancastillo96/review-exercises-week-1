const person = {
    name: "John",
    age: 30
};

// Convert to a JSON string
const jsonString = JSON.stringify(person);

// Show the JSON and its length
console.log('JSON:', jsonString);
console.log('Number of characters:', jsonString.length);


